// WAP to get the value from user
#include<stdio.h>
int main()
{
    float a;
    printf("Enter the value of a: ");
    // taking input using scan function and storing it in variable 'a'
    scanf("%f", &a);
    printf("Value of a is: %f", a);
}